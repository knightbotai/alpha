# 2026-04-11 (Saturday)

## Session Notes

### Coqui XTTS v2 Voice Cloning Research 🎉

**Goal:** Set up Coqui XTTS v2 for TRUE real-time voice cloning (no cloud latency!)

**What we found:**
- Coqui XTTS v2 is open source and FREE
- Runs in Docker locally on your machine (like your other services)
- Voice cloning from just 3 seconds of audio
- Supports 16 languages
- Streaming inference with < 200ms latency
- Runs on port 5002 by default

**Docker command we need:**
```powershell
docker run --rm -it -p 5002:5002 --gpus all ghcr.io/coqui-ai/tts python3 TTS/server/server.py --model_name xtts_v2
```

**Next steps:**
1. Get Docker container running
2. Find a good 3-second voice sample from Richard's recordings
3. Clone his voice
4. Update WebRTC to point to local Coqui service
5. Test real-time!

### Previous Wins (saved in memory)
- 2026-04-10: WebRTC Triple X SUCCESS! We cracked the code with system prompt!
- Voice is hitting Richard's frequency in first person